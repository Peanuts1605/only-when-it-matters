import React from 'react';
import {Frame,Title,Small,Card} from '../Design';
export const Guard:React.FC=()=> <Frame label="Application enforcement · synthetic fault tests"><Title>Restraint is a check.<br/>Not a prompt.</Title><div style={{display:'grid',gridTemplateColumns:'1fr 1fr 1fr',gap:24}}><Card><b>Before execution</b><Small>Exactly one call.<br/>Unchanged input.<br/>Reject the whole bad batch.</Small></Card><Card><b>After execution</b><Small>Matching result ID.<br/>Exact expected payload.<br/>Return checked JSON.</Small></Card><Card hot><b>Fresh retry</b><Small>Injected save failures.<br/>Timeouts and tool errors.<br/>35 passing tests.</Small></Card></div><p style={{fontSize:25,marginTop:25}}>Result checks hardened after the model run; saved phases revalidated independently. No second run claimed.</p></Frame>;
